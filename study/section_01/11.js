// section 01 >> 대문자 찾기

function solution(s) {
  let answer = 0;
  const lower = s.toLowerCase();
  console.log(lower);
  // for (let x of s) {

  // }
}

let str = 'KoreaTimeGood';
console.log(solution(str));
